<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body style="background-color:rgb(32, 32, 32); color:white">

    <nav style="display: flex; justify-content:space-between">
        <div>
            <button><a href="/home">Home</a></button>
            <button><a href="/realtimeproducts">Real time products</a></button>
            <button><a href="/productos">Productos</a></button>
        </div>
        {{#if user}}
        <div>
            <button id="carrito-btn" value="{{carrito}}" style="padding: 0;"><a href="/carrito"><img src="./assets/carrito.png" width="14px" height="14px"></a></button>
            <button><a href="/perfil">Perfil</a></button>
            <button id="logout">Logout</button>
        </div>
        {{else}}
        <div>
            <span id="carrito-btn"></span>
            <button><a href="/login">Login</a></button>
            <button><a href="/registro">Registro</a></button>
        </div>
        {{/if}}
    </nav>
    {{{body}}}
    <script type="module" src="./js/auth.js"></script>
</body>

</html>